import Order from './Order'
import OrderItem from './OrderItem'

export default class CreateOrder {
    constructor() { }
    execute(items: OrderItem[], cpf: string) {
        // const order = new Order({ items, cpf })
        // return order.items.length
    }
}